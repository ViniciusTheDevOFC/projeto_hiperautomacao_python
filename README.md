Para fazer este algoritimo funcionar voce precisa jogar este comando no terminal:

pip install streamlit pandas plotly openpyxl

apos isso, no cmd ou powershell va para a pasta do arquivo seguindo o caminho: cd OneDrive ou Desktop e cd "Projeto 5 periodo em python"
apos isso, utilizar da straemit

streamlit run my_dashboard.py